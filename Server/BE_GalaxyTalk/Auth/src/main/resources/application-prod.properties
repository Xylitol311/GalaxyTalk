# ===================== OAuth2 ?? ?? =====================
spring.security.oauth2.client.registration.naver.redirect-uri=https://i12a503.p.ssafy.io:30001/login/oauth2/code/naver

# ===================== Redis ?? =====================
spring.data.redis.port=6379
spring.data.redis.host=redis
spring.data.redis.password=${REDIS_PASSWORD}

# ===================== ?? ?? =====================
server.address=0.0.0.0
server.port=8080

# ===================== MySQL ?????? ?? =====================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}